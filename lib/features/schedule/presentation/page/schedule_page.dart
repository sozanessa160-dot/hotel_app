import 'package:flutter/material.dart';

class ScheduleUserPage extends StatelessWidget {
  const ScheduleUserPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Schedule Page")));
  }
}
