import 'package:flutter/material.dart';

class BookmarlPage extends StatelessWidget {
  const BookmarlPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("BookMark Page")));
  }
}
